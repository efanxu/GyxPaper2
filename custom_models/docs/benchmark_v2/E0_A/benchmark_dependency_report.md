# Benchmark Dependency Report

Interpreter: `D:\Apps\Miniconda3\envs\env_tslib\python.exe`
Python 3.11.15; torch 2.7.1+cu128; CUDA 12.8; CUDA available True; GPU NVIDIA GeForce GTX 1060; numpy 2.1.2; pandas 2.3.3; scikit-learn 1.7.2.

## Dependency status

The import matrix is in `audit_import_results.json`. All 18 requested TSLib modules and all audited ST-MGPrompt modules imported successfully under the required interpreter. `einops`, `PyWavelets (pywt)`, `sktime`, `datasets`, `huggingface_hub`, and `scipy` were importable. No package was installed, upgraded, downgraded or removed.

## Per-model source and dependencies

- **DLinear** — source `Time-Series-Library/models/DLinear.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **LightTS** — source `Time-Series-Library/models/LightTS.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **TiDE** — source `Time-Series-Library/models/TiDE.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **SegRNN** — source `Time-Series-Library/models/SegRNN.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **Transformer** — source `Time-Series-Library/models/Transformer.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **PatchTST** — source `Time-Series-Library/models/PatchTST.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **iTransformer** — source `Time-Series-Library/models/iTransformer.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **TimeXer** — source `Time-Series-Library/models/TimeXer.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **TimesNet** — source `Time-Series-Library/models/TimesNet.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **MICN** — source `Time-Series-Library/models/MICN.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **WPMixer** — source `Time-Series-Library/models/WPMixer.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **MultiPatchFormer** — source `Time-Series-Library/models/MultiPatchFormer.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **TimeMixer** — source `Time-Series-Library/models/TimeMixer.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **TSMixer** — source `Time-Series-Library/models/TSMixer.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **FreTS** — source `Time-Series-Library/models/FreTS.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **Crossformer** — source `Time-Series-Library/models/Crossformer.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **MSGNet** — source `Time-Series-Library/models/MSGNet.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.
- **TimeFilter** — source `Time-Series-Library/models/TimeFilter.py`; upstream `THUML/Time-Series-Library`; import `IMPORT_OK`; direct dependencies are torch plus the local `layers` modules, and `einops` for models that import it. `WPMixer` additionally imports `pywt` through `layers/DWT_Decomposition.py`; all were importable. License: MIT from `Time-Series-Library/LICENSE`. Protocol status: dependency satisfied for import, benchmark adapter still missing.

## Graph and baseline dependencies

Persistence, MovingAverage, standalone GRU, GCN, STCN/STGCN, DCRNN, Graph WaveNet, MTGNN, AGCRN and STID have no trusted local source or dependency path. They are not `DEPENDENCY_BLOCKED`; they are MISSING/PARTIAL/NAME_CONFLICT. Do not install a package to guess an implementation.

## Optional CUDA / mixed environment risks

TSLib README recommends its own torch environment and mentions optional extensions for models outside this audit (for example `mamba_ssm`). None is required for the 18 audited imports. The current environment is conda-based and the audit did not inspect package provenance beyond import/version; conda/pip mixing risk remains UNKNOWN. No external source or license was fetched.

## Recommended stages

Handle dependency pinning only after E0-B contract freeze. E1-A/E1-B cover baselines/lightweight models; E2-A through E2-D cover TSLib adapters; E3-A through E3-C cover graph source and dependencies. Preserve the local MIT notice when reusing TSLib code.
