# Current batch4 benchmark scope26 change

Status: `ACTIVE`

This change adjusts only the current batch=4 active model scope. It does not
change the frozen training profile, any completed run metadata, or any
historical batch=32 artifact.

## Active scope

- Scope identity: `benchmark_v2_batch4_scope26_seed2026`
- Total benchmark entries: 26
- Trainable entries: 24
- Non-trainable entries: 2
- Excluded from current formal scope: 2

`SegRNN` and `MSGNet` are recorded as
`EXCLUDED_FROM_CURRENT_FORMAL_SCOPE` with reason
`RESOURCE_REQUIREMENT_EXCEEDS_AVAILABLE_FORMAL_HARDWARE`. They are not
`MISSING`, `FAILED`, `BLOCKED`, `PENDING`, `REQUIRED`, or `NOT_READY`.

## Frozen training identity

The existing `uniform_train_batch4_v1` profile is reused without modification:
train=4, validation=4, test=4, gradient accumulation=1, effective train batch=4.
No batch migration or alternate batch4 protocol was created.

## Isolation

- `CURRENT_BATCH4_ACTIVE`: files under
  `custom_models/docs/benchmark_v2/BATCH4/` and the E5 scope27 files named in
  the E5 active scope pointer.
- `CURRENT_BATCH4_HISTORICAL`: the old
  `UNIFORM_BATCH4_PROTOCOL` 28/29 launchers, run-id map, smoke results, OOM
  results, and hardware evidence. These remain read-only.
- `LEGACY_BATCH32_READ_ONLY`: the old E5 29-entry manifest/readiness/launcher,
  formal A8 artifacts, and any batch=32 result artifacts.
- `SOURCE_CODE_ONLY`: SegRNN and MSGNet implementations in `custom_models/src`
  and `Time-Series-Library`.
- `SUPERSEDED_SCRIPT`: old 28/29 launchers remain on disk but do not control
  the current batch4 scope.

No original-benchmark Full Linux launcher is generated by this change.
